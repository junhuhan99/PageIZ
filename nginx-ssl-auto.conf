# HTTP to HTTPS redirect
server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS main site
server {
    listen 443 ssl http2;
    server_name pageiz.me www.pageiz.me;

    ssl_certificate /etc/letsencrypt/live/pageiz.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pageiz.me/privkey.pem;

    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS wildcard subdomains
server {
    listen 443 ssl http2;
    server_name ~^(?<subdomain>.+)\.pageiz\.me$;

    # Use wildcard certificate or individual certificate
    ssl_certificate /etc/letsencrypt/live/pageiz.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pageiz.me/privkey.pem;

    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS docs subdomain
server {
    listen 443 ssl http2;
    server_name docs.pageiz.me;

    ssl_certificate /etc/letsencrypt/live/docs.pageiz.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/docs.pageiz.me/privkey.pem;

    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    return 301 https://pageiz.me/docs;
}
